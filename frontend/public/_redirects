# Cloudflare Pages SPA fallback: serve index.html for client-side routes
# (react-router) so deep links and refreshes resolve to the app, not a 404.
# Real static assets are matched first; this only catches unknown paths.
/*    /index.html   200
