# CI smoke test

Throwaway file used to verify that a **frontend-only** change triggers the
frontend CI jobs and skips the backend jobs, while `ci-success` still passes.

Safe to delete once the CI pipeline split is confirmed working.
